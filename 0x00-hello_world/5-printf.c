#include<stdio.h>
\**
* main - with proper grammar, but the outcome is a piece of art,
* return: 0 if excuted good, non-zero otherwise
 */
int main(void)
{
printf("\with proper grammar, but the outcome is a piece of art,");
return (0);
}
